const Button = () => {
  return (
    <button className="bg-white hover:bg-black/10 hover:text-white text-[var(--Light-red)] px-5 py-2.5 rounded-full text-[15px]">
      Sign Up
    </button>
  );
};

export default Button;
